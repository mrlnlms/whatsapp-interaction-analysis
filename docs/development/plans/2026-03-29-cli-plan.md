# CLI `whatsapp-interaction` Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** CLI unificado que expõe o pipeline de preparação e processamento como comandos de terminal.

**Architecture:** Typer app com 2 grupos de comandos (prepare, process) + run (orquestrador) + status. Cada comando importa e chama `main()` dos módulos/scripts existentes. Zero alteração no código existente.

**Tech Stack:** Typer, Rich, Python 3.11+

**Spec:** `docs/specs/2026-03-29-cli-design.md`

---

## File Structure

| Arquivo | Responsabilidade |
|---------|-----------------|
| `src/cli/__init__.py` | App Typer principal, registra grupos prepare/process |
| `src/cli/__main__.py` | Entry point para `python -m src.cli` |
| `src/cli/helpers.py` | `load_config()`, validações compartilhadas |
| `src/cli/prepare.py` | Comandos: prepare (all), clean, wrangle, transcribe |
| `src/cli/process.py` | Comandos: process (all), sentiment, embeddings |
| `src/cli/status.py` | Comando: status |
| `pyproject.toml` | Entry point `whatsapp-interaction` |
| `tests/test_cli.py` | Testes do CLI |

---

## Chunk 1: Foundation

### Task 1: Dependências + pyproject.toml

**Files:**
- Modify: `requirements.txt`
- Create: `pyproject.toml`

- [ ] **Step 1: Adicionar typer ao requirements.txt**

Adicionar ao final de `requirements.txt`:
```
# CLI
typer[all]>=0.9.0
```

- [ ] **Step 2: Criar pyproject.toml**

Criar `pyproject.toml`:
```toml
[project]
name = "whatsapp-interaction-analysis"
version = "0.1.0"
description = "End-to-end data science pipeline for WhatsApp conversation analysis"
requires-python = ">=3.11"

[project.scripts]
whatsapp-interaction = "src.cli:app"
```

- [ ] **Step 3: Instalar no venv**

```bash
source venv/bin/activate
pip install typer[all]
pip install -e .
```

- [ ] **Step 4: Commit**

```bash
git add requirements.txt pyproject.toml
```
Mensagem: `feat: adiciona typer e pyproject.toml para CLI`

---

### Task 2: Helpers + App principal

**Files:**
- Create: `src/cli/__init__.py`
- Create: `src/cli/__main__.py`
- Create: `src/cli/helpers.py`
- Test: `tests/test_cli.py`

- [ ] **Step 1: Escrever teste para load_config**

Criar `tests/test_cli.py`:
```python
"""Testes para o CLI whatsapp-interaction."""

import pytest
from unittest.mock import patch
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent.parent / "src"))


class TestLoadConfig:
    def test_load_config_without_env_returns_error(self):
        """load_config retorna None quando .env não existe."""
        with patch.dict("os.environ", {}, clear=True):
            # Re-import pra forçar reload sem env vars
            from cli.helpers import load_config
            result = load_config()
            assert result is None

    def test_load_config_with_env_returns_paths(self, tmp_path):
        """load_config retorna PATHS quando .env está correto."""
        env_vars = {
            "PROJECT_ROOT": str(tmp_path),
            "DATA_FOLDER": "test_export"
        }
        # Criar dirs necessários
        (tmp_path / "data" / "raw" / "test_export").mkdir(parents=True)
        (tmp_path / "data" / "interim" / "test_export").mkdir(parents=True)
        (tmp_path / "data" / "processed" / "test_export").mkdir(parents=True)

        with patch.dict("os.environ", env_vars):
            from cli.helpers import load_config
            result = load_config()
            # Pode retornar None se config.py falhar no reload
            # O teste principal é que não crasheia
```

- [ ] **Step 2: Rodar teste pra ver falhar**

```bash
source venv/bin/activate
pytest tests/test_cli.py -v
```
Expected: FAIL (módulo não existe)

- [ ] **Step 3: Criar src/cli/helpers.py**

```python
"""Helpers compartilhados do CLI."""

import os
import typer
from rich.console import Console
from pathlib import Path

console = Console()


def load_config():
    """
    Carrega configuração do projeto.
    Retorna (PATHS, PROJECT_ROOT) ou None se falhar.
    """
    try:
        from config import PATHS, PROJECT_ROOT
        return PATHS, PROJECT_ROOT
    except EnvironmentError as e:
        console.print(f"[red]Erro de configuração:[/red] {e}")
        return None


def require_config():
    """
    Carrega config ou aborta com mensagem clara.
    Uso: PATHS, PROJECT_ROOT = require_config()
    """
    result = load_config()
    if result is None:
        raise typer.Exit(1)
    return result


def require_file(path: Path, description: str, hint: str = ""):
    """Valida que um arquivo existe ou aborta com mensagem."""
    if not path.exists():
        console.print(f"[red]Arquivo não encontrado:[/red] {description}")
        console.print(f"  Path: {path}")
        if hint:
            console.print(f"  [yellow]Dica:[/yellow] {hint}")
        raise typer.Exit(1)


def validate_steps(steps_str: str, valid_steps: list) -> list:
    """
    Valida e parseia string de steps separados por vírgula.
    Retorna lista de steps válidos ou aborta.
    """
    if not steps_str:
        return valid_steps

    requested = [s.strip() for s in steps_str.split(",")]
    invalid = [s for s in requested if s not in valid_steps]

    if invalid:
        console.print(f"[red]Etapas inválidas:[/red] {', '.join(invalid)}")
        console.print(f"[yellow]Etapas disponíveis:[/yellow] {', '.join(valid_steps)}")
        raise typer.Exit(1)

    return requested
```

- [ ] **Step 4: Criar src/cli/__init__.py**

```python
"""CLI whatsapp-interaction — pipeline de análise de conversas WhatsApp."""

import typer

app = typer.Typer(
    name="whatsapp-interaction",
    help="Pipeline de análise de conversas WhatsApp.",
    no_args_is_help=True,
)
```

- [ ] **Step 5: Criar src/cli/__main__.py**

```python
"""Entry point para python -m src.cli."""

from src.cli import app

if __name__ == "__main__":
    app()
```

- [ ] **Step 6: Rodar testes**

```bash
pytest tests/test_cli.py -v
```

- [ ] **Step 7: Verificar que o CLI responde**

```bash
python -m src.cli --help
```
Expected: Mensagem de help do Typer

- [ ] **Step 8: Commit**

```bash
git add src/cli/ tests/test_cli.py
```
Mensagem: `feat: CLI foundation com helpers e app principal`

---

## Chunk 2: Prepare commands

### Task 3: Comando `prepare clean`

**Files:**
- Create: `src/cli/prepare.py`
- Modify: `src/cli/__init__.py`
- Test: `tests/test_cli.py`

- [ ] **Step 1: Escrever teste para validate_steps**

Adicionar a `tests/test_cli.py`:
```python
class TestValidateSteps:
    def test_returns_all_when_empty(self):
        from cli.helpers import validate_steps
        valid = ["a", "b", "c"]
        assert validate_steps("", valid) == valid

    def test_returns_requested_steps(self):
        from cli.helpers import validate_steps
        valid = ["a", "b", "c"]
        assert validate_steps("a,c", valid) == ["a", "c"]

    def test_exits_on_invalid_step(self):
        from cli.helpers import validate_steps
        valid = ["a", "b", "c"]
        with pytest.raises(SystemExit):
            validate_steps("a,x", valid)
```

- [ ] **Step 2: Rodar testes**

```bash
pytest tests/test_cli.py::TestValidateSteps -v
```

- [ ] **Step 3: Criar src/cli/prepare.py**

```python
"""Comandos de preparação: clean, wrangle, transcribe."""

import time
import typer
from typing import Optional
from rich.console import Console

console = Console()

prepare_app = typer.Typer(
    name="prepare",
    help="Preparação de dados: limpeza, parsing, transcrição.",
    invoke_without_command=True,
)


@prepare_app.callback()
def prepare_all(ctx: typer.Context):
    """Roda pipeline completo: clean → wrangle → transcribe (se API key existir)."""
    if ctx.invoked_subcommand is not None:
        return

    from cli.helpers import require_config
    PATHS, _ = require_config()

    start = time.time()

    # Clean
    console.print("\n[bold blue]1/3 — Limpeza[/bold blue]")
    _run_clean(PATHS, steps=None)

    # Wrangle
    console.print("\n[bold blue]2/3 — Wrangling[/bold blue]")
    _run_wrangle(PATHS, steps=None)

    # Transcribe (condicional)
    import os
    if os.getenv("GROQ_API_KEY"):
        console.print("\n[bold blue]3/3 — Transcrição[/bold blue]")
        _run_transcribe()
    else:
        console.print("\n[yellow]3/3 — Transcrição pulada (GROQ_API_KEY não encontrada)[/yellow]")

    elapsed = time.time() - start
    console.print(f"\n[bold green]Preparação concluída em {elapsed:.1f}s[/bold green]")


@prepare_app.command()
def clean(
    steps: Optional[str] = typer.Option(
        None, "--steps", "-s",
        help="Etapas separadas por vírgula. Default: todas."
    ),
):
    """Pipeline de limpeza (7 etapas)."""
    from cli.helpers import require_config
    PATHS, _ = require_config()
    _run_clean(PATHS, steps)


@prepare_app.command()
def wrangle(
    steps: Optional[str] = typer.Option(
        None, "--steps", "-s",
        help="Etapas separadas por vírgula. Default: todas."
    ),
):
    """Parsing, classificação, mídia, transcrição, enriquecimento, export."""
    from cli.helpers import require_config
    PATHS, _ = require_config()
    _run_wrangle(PATHS, steps)


@prepare_app.command()
def transcribe():
    """Transcrição de áudios/vídeos via Groq/Whisper."""
    _run_transcribe()


def _run_clean(PATHS: dict, steps: Optional[str]):
    """Executa pipeline de limpeza."""
    import sys
    sys.path.insert(0, str(PATHS["src"].parent / "src"))

    from cli.helpers import require_file, validate_steps
    from cleaning import run_pipeline, CLEANING_STEPS

    default_order = [
        "u200e", "anonymize", "timestamps", "indentation",
        "empty_lines", "whitespace", "empty_timestamps"
    ]
    order = validate_steps(steps or "", default_order)

    raw_file = PATHS["raw"]
    require_file(raw_file, "Arquivo raw do WhatsApp", "Coloque o export em data/raw/{DATA_FOLDER}/raw-data.txt")

    result = run_pipeline(order=order, raw_file=raw_file, output_dir=PATHS["interim"])
    console.print(f"[green]Limpeza concluída:[/green] redução de {result['totals']['total_percent']:.1f}%")


def _run_wrangle(PATHS: dict, steps: Optional[str]):
    """Executa pipeline de wrangling."""
    import sys
    sys.path.insert(0, str(PATHS["src"].parent / "src"))

    from cli.helpers import require_file, validate_steps

    default_order = ["parse", "classify", "media", "transcriptions", "enrich", "export"]
    order = validate_steps(steps or "", default_order)

    # Encontra último arquivo de cleaning
    interim_dir = PATHS["interim"]
    input_file = interim_dir / "raw-data_cln7.txt"
    require_file(input_file, "Arquivo limpo (cln7)", "Rode primeiro: whatsapp-interaction prepare clean")

    from wrangling import run_wrangling_pipeline

    result = run_wrangling_pipeline(
        order=order,
        input_file=input_file,
        output_dir=PATHS["processed"],
        media_dir=PATHS["media"],
        transcription_file=PATHS["processed"] / "transcriptions.csv",
    )
    console.print(f"[green]Wrangling concluído:[/green] {len(result['df'])} mensagens processadas")


def _run_transcribe():
    """Executa transcrição de mídia."""
    import os
    from cli.helpers import console

    api_key = os.getenv("GROQ_API_KEY")
    if not api_key:
        console.print("[red]GROQ_API_KEY não encontrada no .env[/red]")
        console.print("[yellow]Adicione: GROQ_API_KEY=sua_chave_aqui ao arquivo .env[/yellow]")
        raise typer.Exit(1)

    import sys
    import importlib.util
    from cli.helpers import require_config
    PATHS, PROJECT_ROOT = require_config()

    # Importa o script transcribe_media.py diretamente
    spec = importlib.util.spec_from_file_location(
        "transcribe_media",
        PROJECT_ROOT / "scripts" / "transcribe_media.py"
    )
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    mod.main()

    console.print("[green]Transcrição concluída[/green]")
```

- [ ] **Step 4: Registrar prepare_app no __init__.py**

Modificar `src/cli/__init__.py`:
```python
"""CLI whatsapp-interaction — pipeline de análise de conversas WhatsApp."""

import typer

app = typer.Typer(
    name="whatsapp-interaction",
    help="Pipeline de análise de conversas WhatsApp.",
    no_args_is_help=True,
)

from src.cli.prepare import prepare_app
app.add_typer(prepare_app)
```

- [ ] **Step 5: Testar help**

```bash
python -m src.cli prepare --help
python -m src.cli prepare clean --help
```

- [ ] **Step 6: Commit**

Mensagem: `feat: CLI prepare com clean, wrangle e transcribe`

---

## Chunk 3: Process commands

### Task 4: Comandos `process sentiment` e `process embeddings`

**Files:**
- Create: `src/cli/process.py`
- Modify: `src/cli/__init__.py`

- [ ] **Step 1: Criar src/cli/process.py**

```python
"""Comandos de processamento: sentiment, embeddings."""

import time
import typer
from typing import Optional
from enum import Enum
from rich.console import Console
import importlib.util
from pathlib import Path

console = Console()


class SentimentModel(str, Enum):
    distilbert = "distilbert"
    deberta = "deberta"
    roberta = "roberta"
    ensemble = "ensemble"


class EmbeddingsModel(str, Enum):
    mpnet = "mpnet"
    minilm = "minilm"
    distiluse = "distiluse"


# Mapeamento modelo → script
SENTIMENT_SCRIPTS = {
    "distilbert": "sentiment_distilbert.py",
    "deberta": "sentiment_deberta.py",
    "roberta": "sentiment_twitter_roberta.py",
    "ensemble": "sentiment_ensemble.py",
}

EMBEDDINGS_SCRIPTS = {
    "mpnet": "generate_embeddings.py",
    "minilm": "generate_embeddings_minilm.py",
    "distiluse": "generate_embeddings_distiluse.py",
}


process_app = typer.Typer(
    name="process",
    help="Processamento ML: sentiment analysis, embeddings.",
    invoke_without_command=True,
)


def _load_and_run_script(script_name: str):
    """Importa um script de scripts/ e chama main()."""
    from cli.helpers import require_config
    _, PROJECT_ROOT = require_config()

    script_path = PROJECT_ROOT / "scripts" / script_name
    if not script_path.exists():
        console.print(f"[red]Script não encontrado:[/red] {script_path}")
        raise typer.Exit(1)

    spec = importlib.util.spec_from_file_location(
        script_path.stem, script_path
    )
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    mod.main()


def _require_processed_data():
    """Valida que messages.parquet existe."""
    from cli.helpers import require_config, require_file
    PATHS, _ = require_config()
    parquet = PATHS["processed"] / "messages.parquet"
    require_file(parquet, "Dados processados (messages.parquet)", "Rode primeiro: whatsapp-interaction prepare")
    return PATHS


@process_app.callback()
def process_all(ctx: typer.Context):
    """Roda sentiment (ensemble) + embeddings (mpnet)."""
    if ctx.invoked_subcommand is not None:
        return

    _require_processed_data()
    start = time.time()

    console.print("\n[bold blue]1/2 — Sentiment analysis (ensemble)[/bold blue]")
    _load_and_run_script(SENTIMENT_SCRIPTS["ensemble"])

    console.print("\n[bold blue]2/2 — Embeddings (mpnet)[/bold blue]")
    _load_and_run_script(EMBEDDINGS_SCRIPTS["mpnet"])

    elapsed = time.time() - start
    console.print(f"\n[bold green]Processamento concluído em {elapsed:.1f}s[/bold green]")


@process_app.command()
def sentiment(
    model: SentimentModel = typer.Option(
        SentimentModel.ensemble, "--model", "-m",
        help="Modelo de sentiment analysis."
    ),
):
    """Análise de sentimento."""
    _require_processed_data()
    console.print(f"[bold blue]Sentiment analysis ({model.value})[/bold blue]")
    _load_and_run_script(SENTIMENT_SCRIPTS[model.value])
    console.print(f"[green]Sentiment ({model.value}) concluído[/green]")


@process_app.command()
def embeddings(
    model: EmbeddingsModel = typer.Option(
        EmbeddingsModel.mpnet, "--model", "-m",
        help="Modelo de embeddings."
    ),
):
    """Geração de embeddings semânticos."""
    _require_processed_data()
    console.print(f"[bold blue]Embeddings ({model.value})[/bold blue]")
    _load_and_run_script(EMBEDDINGS_SCRIPTS[model.value])
    console.print(f"[green]Embeddings ({model.value}) concluído[/green]")
```

- [ ] **Step 2: Registrar no __init__.py**

Adicionar a `src/cli/__init__.py`:
```python
from src.cli.process import process_app
app.add_typer(process_app)
```

- [ ] **Step 3: Testar help**

```bash
python -m src.cli process --help
python -m src.cli process sentiment --help
python -m src.cli process embeddings --help
```

- [ ] **Step 4: Commit**

Mensagem: `feat: CLI process com sentiment e embeddings`

---

### Task 5: Comandos `run` e `status`

**Files:**
- Create: `src/cli/status.py`
- Modify: `src/cli/__init__.py`

- [ ] **Step 1: Criar src/cli/status.py**

```python
"""Comando status — mostra estado dos dados processados."""

import typer
from pathlib import Path
from rich.console import Console
from rich.table import Table

console = Console()


def status():
    """Mostra o estado atual dos dados processados."""
    from cli.helpers import load_config

    result = load_config()
    if result is None:
        console.print("[yellow]Sem .env configurado. Mostrando paths relativos.[/yellow]\n")
        # Tenta paths relativos ao cwd
        base = Path.cwd()
        interim = base / "data" / "interim"
        processed = base / "data" / "processed"
    else:
        PATHS, _ = result
        interim = PATHS["interim"]
        processed = PATHS["processed"]

    table = Table(title="Status do Pipeline")
    table.add_column("Etapa", style="bold")
    table.add_column("Status")
    table.add_column("Detalhes")

    # Cleaning
    cln_files = sorted(interim.glob("raw-data_cln*.txt")) if interim.exists() else []
    if cln_files:
        table.add_row("Cleaning", f"[green]{len(cln_files)}/7 etapas[/green]", cln_files[-1].name)
    else:
        table.add_row("Cleaning", "[red]Não executado[/red]", "")

    # Wrangling
    parquet = processed / "messages.parquet"
    csv = processed / "messages.csv"
    if parquet.exists():
        size_mb = parquet.stat().st_size / (1024 * 1024)
        table.add_row("Wrangling", "[green]Completo[/green]", f"messages.parquet ({size_mb:.1f} MB)")
    elif csv.exists():
        table.add_row("Wrangling", "[green]Completo[/green]", "messages.csv")
    else:
        table.add_row("Wrangling", "[red]Não executado[/red]", "")

    # Transcrição
    transcriptions = processed / "transcriptions.csv"
    if transcriptions.exists():
        table.add_row("Transcrição", "[green]Encontrado[/green]", "transcriptions.csv")
    else:
        table.add_row("Transcrição", "[yellow]Não encontrado[/yellow]", "")

    # Sentiment
    sentiment_models = []
    for name in ["roberta", "distilbert", "deberta", "ensemble"]:
        meta = processed / f"sentiment_{name}_metadata.json"
        if meta.exists():
            sentiment_models.append(name)
    if sentiment_models:
        table.add_row("Sentiment", f"[green]{len(sentiment_models)} modelo(s)[/green]", ", ".join(sentiment_models))
    else:
        table.add_row("Sentiment", "[yellow]Não executado[/yellow]", "")

    # Embeddings
    embedding_models = []
    for name, file in [("mpnet", "message_embeddings_mpnet.npy"), ("minilm", "message_embeddings_minilm.npy"), ("distiluse", "message_embeddings_distiluse.npy")]:
        if (processed / file).exists():
            embedding_models.append(name)
    if embedding_models:
        table.add_row("Embeddings", f"[green]{len(embedding_models)} modelo(s)[/green]", ", ".join(embedding_models))
    else:
        table.add_row("Embeddings", "[yellow]Não executado[/yellow]", "")

    console.print(table)
```

- [ ] **Step 2: Adicionar run e status ao __init__.py**

Modificar `src/cli/__init__.py` (versão final):
```python
"""CLI whatsapp-interaction — pipeline de análise de conversas WhatsApp."""

import time
import typer
from typing import Optional
from rich.console import Console

console = Console()

app = typer.Typer(
    name="whatsapp-interaction",
    help="Pipeline de análise de conversas WhatsApp.",
    no_args_is_help=True,
)

# Registra grupos
from src.cli.prepare import prepare_app
from src.cli.process import process_app
from src.cli.status import status

app.add_typer(prepare_app)
app.add_typer(process_app)
app.command()(status)


@app.command()
def run(
    skip_transcribe: bool = typer.Option(False, "--skip-transcribe", help="Pular transcrição."),
    skip_process: bool = typer.Option(False, "--skip-process", help="Pular sentiment/embeddings."),
):
    """Pipeline completo: prepare + process."""
    from src.cli.prepare import prepare_app, _run_clean, _run_wrangle, _run_transcribe
    from src.cli.helpers import require_config
    import os

    PATHS, _ = require_config()
    start = time.time()

    # Prepare
    console.print("\n[bold]═══ PREPARAÇÃO ═══[/bold]")
    console.print("\n[bold blue]Limpeza[/bold blue]")
    _run_clean(PATHS, steps=None)

    console.print("\n[bold blue]Wrangling[/bold blue]")
    _run_wrangle(PATHS, steps=None)

    if not skip_transcribe and os.getenv("GROQ_API_KEY"):
        console.print("\n[bold blue]Transcrição[/bold blue]")
        _run_transcribe()
    elif not skip_transcribe:
        console.print("\n[yellow]Transcrição pulada (GROQ_API_KEY não encontrada)[/yellow]")

    # Process
    if not skip_process:
        console.print("\n[bold]═══ PROCESSAMENTO ═══[/bold]")
        from src.cli.process import _load_and_run_script, SENTIMENT_SCRIPTS, EMBEDDINGS_SCRIPTS

        console.print("\n[bold blue]Sentiment (ensemble)[/bold blue]")
        _load_and_run_script(SENTIMENT_SCRIPTS["ensemble"])

        console.print("\n[bold blue]Embeddings (mpnet)[/bold blue]")
        _load_and_run_script(EMBEDDINGS_SCRIPTS["mpnet"])

    elapsed = time.time() - start
    console.print(f"\n[bold green]Pipeline concluído em {elapsed:.1f}s[/bold green]")
```

- [ ] **Step 3: Testar help de todos os comandos**

```bash
python -m src.cli --help
python -m src.cli run --help
python -m src.cli status
python -m src.cli prepare --help
python -m src.cli process --help
```

- [ ] **Step 4: Commit**

Mensagem: `feat: CLI run e status`

---

## Chunk 4: Testes e finalização

### Task 6: Testes do CLI

**Files:**
- Modify: `tests/test_cli.py`

- [ ] **Step 1: Adicionar testes de integração do CLI**

Adicionar a `tests/test_cli.py`:
```python
from typer.testing import CliRunner


class TestCLIHelp:
    """Testa que todos os comandos respondem --help sem crash."""

    def _get_app(self):
        from src.cli import app
        return app

    def test_main_help(self):
        runner = CliRunner()
        result = runner.invoke(self._get_app(), ["--help"])
        assert result.exit_code == 0
        assert "prepare" in result.output
        assert "process" in result.output
        assert "status" in result.output
        assert "run" in result.output

    def test_prepare_help(self):
        runner = CliRunner()
        result = runner.invoke(self._get_app(), ["prepare", "--help"])
        assert result.exit_code == 0
        assert "clean" in result.output
        assert "wrangle" in result.output
        assert "transcribe" in result.output

    def test_process_help(self):
        runner = CliRunner()
        result = runner.invoke(self._get_app(), ["process", "--help"])
        assert result.exit_code == 0
        assert "sentiment" in result.output
        assert "embeddings" in result.output

    def test_clean_help(self):
        runner = CliRunner()
        result = runner.invoke(self._get_app(), ["prepare", "clean", "--help"])
        assert result.exit_code == 0
        assert "--steps" in result.output

    def test_sentiment_help(self):
        runner = CliRunner()
        result = runner.invoke(self._get_app(), ["process", "sentiment", "--help"])
        assert result.exit_code == 0
        assert "--model" in result.output

    def test_status_without_env(self):
        runner = CliRunner()
        result = runner.invoke(self._get_app(), ["status"])
        # Deve funcionar mesmo sem .env
        assert result.exit_code == 0
```

- [ ] **Step 2: Rodar todos os testes**

```bash
pytest tests/ -v
```
Expected: Todos passam (63 antigos + novos do CLI)

- [ ] **Step 3: Commit**

Mensagem: `feat: testes do CLI`

---

### Task 7: Atualizar README e docs

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Adicionar seção CLI ao README**

Na seção Quick Start, após o bloco existente, adicionar:

```markdown
### CLI

```bash
pip install -e .

# Pipeline completo
whatsapp-interaction run

# Só preparação
whatsapp-interaction prepare

# Etapas individuais
whatsapp-interaction prepare clean --steps u200e,anonymize
whatsapp-interaction process sentiment --model deberta

# Ver estado atual
whatsapp-interaction status
```
```

- [ ] **Step 2: Commit**

Mensagem: `docs: adiciona CLI ao README`

- [ ] **Step 3: Push**

```bash
git push origin main
```

---

## Verificação Final

Após todos os commits:

```bash
# Help funciona
python -m src.cli --help

# Status funciona sem dados
python -m src.cli status

# Testes passam
pytest tests/ -v

# Se tiver dados, pipeline funciona
python -m src.cli prepare clean
python -m src.cli prepare wrangle
python -m src.cli process sentiment --model deberta
```
